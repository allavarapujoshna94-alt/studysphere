# API URL used by the React app.
# Local default is already http://localhost:5000/api, so this file is optional.
# For deployment where the API has a different URL, set it here:
VITE_API_URL=http://localhost:5000/api
